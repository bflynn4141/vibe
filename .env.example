# Vibe Platform Environment Variables
# Copy to .env.local and fill in values
# CRITICAL: All secrets MUST be set in production (no fallback defaults)

# ============================================================
# REQUIRED - Vercel KV (Redis)
# ============================================================
KV_REST_API_URL=your-kv-url
KV_REST_API_TOKEN=your-kv-token

# ============================================================
# REQUIRED - Authentication & Sessions
# ============================================================
# Session signing secret - generate with: openssl rand -base64 32
VIBE_SESSION_SECRET=your-session-secret

# Auth secret for token validation
VIBE_AUTH_SECRET=your-auth-secret

# System secret (fallback for session if VIBE_SESSION_SECRET not set)
VIBE_SYSTEM_SECRET=your-system-secret

# Admin secret for bypass operations (e.g., alpha download bypass)
# Generate with: openssl rand -base64 32
VIBE_ADMIN_SECRET=your-admin-secret

# ============================================================
# REQUIRED - PostgreSQL
# ============================================================
POSTGRES_URL=postgres://user:password@host/database

# ============================================================
# OPTIONAL - GitHub OAuth
# ============================================================
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
GITHUB_REDIRECT_URI=https://www.slashvibe.dev/api/auth/github/callback

# ============================================================
# PRODUCTION VERIFICATION CHECKLIST
# ============================================================
# Before deploying, verify in Vercel Dashboard:
# 1. KV_REST_API_URL - Vercel KV connection URL
# 2. KV_REST_API_TOKEN - Vercel KV auth token
# 3. VIBE_SESSION_SECRET - Random 32+ byte secret
# 4. VIBE_AUTH_SECRET - Random 32+ byte secret
# 5. POSTGRES_URL - Vercel Postgres connection string
#
# Security notes:
# - NEVER commit real secrets to git
# - Use different secrets for production vs preview
# - Rotate secrets if exposed
# - Without secrets, session tokens will fail to generate/validate
